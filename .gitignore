node_modules/
.env
.env.local
.env.*.local
.DS_Store
*.log
coverage/
dist/
.vscode/
.idea/
# Claude Code / local agent memory — not part of the repo
memory/
# Local Claude Code config + Playwright-MCP cache + screenshot scratch
.claude/
.playwright-mcp/
homepage-*.png
page-*.png
# Legacy key-store file (pre-DB) — never commit
/data/api-keys.json
/data/

